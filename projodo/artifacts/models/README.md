# Model Artifacts

Use this directory for trained models and model-adjacent files produced by this project.

```text
models/
  segmenter/                # segmentation weights such as segmenter_unet_small.pth
  classifiers/              # classifier estimators such as color_clf.pkl and rank_clf.pkl
    classes/                # class lookup arrays used with the classifiers
```
