# Evaluation Scripts

Place validation, model diagnostics, and experiment comparison scripts here.

Quick scene-level card check on labeled train images:

```powershell
python scripts/evaluation/evaluate_scene_pipeline.py --limit 12 --save-overlays
```
